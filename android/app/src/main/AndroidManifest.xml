<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Android App Links for the official site. Verified against
                public/.well-known/assetlinks.json, which must list the SHA-256 of
                the certificate the installed APK is actually signed with — for a
                Play release that is Google's app signing key, not the upload key.
                If the fingerprints do not match, Android silently falls back to
                opening these links in the browser.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="azkaralmumin.netlify.app" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Firebase Analytics must not collect an advertising ID; the Play
             Data Safety declaration states the app collects none. -->
        <meta-data android:name="google_analytics_adid_collection_enabled" android:value="false" />
    </application>

    <!-- ===================== Permissions ===================== -->

    <!-- Fetching Quran audio, prayer-time APIs and the over-the-air web bundle.
         The app itself opens and runs fully without any of them. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Adhan, adhkar and qiyam reminders.
         POST_NOTIFICATIONS is the Android 13+ runtime permission; it is
         requested from JS via @capacitor/local-notifications. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Prayer reminders have to fire at the exact minute of the adhan, so they
         are scheduled as exact alarms. SCHEDULE_EXACT_ALARM is the user-grantable
         form; USE_EXACT_ALARM is deliberately NOT declared, because Google Play
         restricts it to alarm-clock and calendar apps and using it here would
         risk a policy rejection. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Re-arms today's prayer notifications after a reboot, otherwise every
         scheduled alarm is lost when the phone restarts. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Prayer times and the qibla direction are computed from the user's
         coordinates, obtained through navigator.geolocation in the WebView.
         Capacitor's BridgeWebChromeClient asks for these at runtime the first
         time the page requests a position; the app also offers manual city
         selection, so denying them only disables automatic detection. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- ===================== Hardware ===================== -->
    <!--
        Declaring a location permission makes Play imply a hardware requirement
        and hide the app from devices without GPS or a compass. Everything here
        is optional, so the listing stays available on every Android phone and
        tablet; the features degrade instead of blocking install.
    -->
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
</manifest>
